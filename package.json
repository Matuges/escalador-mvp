{
  "name": "sunshine",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "docker compose up -d && concurrently -n api,front -c blue,green \"npm run start:dev -w api\" \" npm run dev -w front\""
  },
  "keywords": [],
  "type": "commonjs",
  "workspaces": [
    "api",
    "front"
  ],
  "allowScripts": {
    "esbuild@0.25.12": true
  },
  "devDependencies": {
    "concurrently": "10.0.5"
  }
}
